#ifndef PARTITION_TYPE_H
#define PARTITION_TYPE_H

#include <vector>
#include <cstdint>

typedef std::vector<int32_t> HMR_NODE;
typedef std::vector<HMR_NODE> HMR_NODES;

#endif // PARTITION_TYPE_H