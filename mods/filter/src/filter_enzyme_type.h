#ifndef FILTER_ENZYME_TYPE_H
#define FILTER_ENZYME_TYPE_H

#include <cstdint>

typedef struct FILTER_ENZYME_RANGE
{
    size_t start, end, length;
} FILTER_ENZYME_RANGE;

#endif // FILTER_ENZYME_TYPE_H